
# MySQL Python CRUD App

A simple CRUD (Create, Read,insert) application using Python and MySQL.

## Requirements
- Python 3
- MySQL Server
- mysql-connector-python

## How to Run
1. Install dependencies:
   pip install mysql-connector-python

2. Update database config inside the code

3. Run the script:
  mysql_python-crud-app
